{
  "name": "@heyjule/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared domain models & TypeScript types used across apps and services.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"TODO: lint shared-types\" && exit 0",
    "typecheck": "echo \"TODO: typecheck shared-types\" && exit 0"
  }
}
