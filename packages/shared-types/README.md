# @heyjule/shared-types

Shared **domain models & TypeScript types** — the common vocabulary across the repo.

Examples of what lives here: `Patient`, `Doctor`, `WearableSample`, `SymptomEvent`,
`Reminder`, `LinkCode`, `Report`. One source of truth so apps and services agree on shapes.
