# @heyjule/api-client

Typed **client SDK** for `services/api`.

Shared by `apps/patient-mobile` and `apps/doctor-web` so both talk to the connection
layer through one typed interface. Built on `@heyjule/shared-types`.
