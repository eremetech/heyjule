{
  "name": "heyjule",
  "version": "0.0.0",
  "private": true,
  "description": "Connection layer between patients' wearable + conversational data and their doctors.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "react-native-audio-api@0.10.3": "patches/react-native-audio-api@0.10.3.patch",
      "expo-constants@18.0.13": "patches/expo-constants@18.0.13.patch"
    }
  }
}
