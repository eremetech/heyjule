# @heyjule/reports

**Interactive summary/report generation.**

## Responsibilities

- Combine aggregated wearable data + logged symptoms into an **interactive summary/report**.
- Produce the report payload that `apps/doctor-web` renders when a patient is linked.

## Feeds

- `services/api`, which delivers the report to the linked doctor.
